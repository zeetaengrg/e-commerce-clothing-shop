import React from "react";
import { Btn } from "./Button.styles";

const Button = () => {
  return (
    <React.Fragment>
      <Btn>shop now</Btn>
    </React.Fragment>
  );
};

export default Button;
